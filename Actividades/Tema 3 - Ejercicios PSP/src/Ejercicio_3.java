public class Ejercicio_3 {

    /**
     *
     * Socket: Esta clase implementa sockets de clientes. Este es un punto
     * y final para la comunicación entre dos máquinas. Su función la realiza una instancia de la clase SocketImpl.
     *
     * ServerSocket: Esta clase sirve como socket único para cada servidor,
     * y permite crear objetos Socket para atender las peticiones.
     *
     * DatagramSocket: Es un socket para enviar mensajes mediante UDP.
     *
     * DatagramPacket: Es el medio por el que se transmite la información en los Sockets Datagram.
     *
     */
}
